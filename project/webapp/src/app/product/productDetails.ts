export interface ProductDetailsPojo {
    productCode: number,
    quantity: number,
}

export interface ProductDetails {
    ratePerQuantity: number,
    productName: string,
    productCode: number,
    quantity: number,
}

